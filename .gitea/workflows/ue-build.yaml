name: UE Automation Build
run-name: ${{ gitea.actor }} is building the project
on: [push]

jobs:
  build-linux-job:
    runs-on: ue-runner
    container:
      image: gitea/runner-images:ubuntu-latest
      volumes:
        - /media/blacksnow/devssd/code/UnrealEngine:/opt/UE
    
    steps:
      - name: Install System Dependencies
        run: |
          apt-get update
          apt-get install -y git git-lfs wget apt-transport-https software-properties-common iputils-ping
          # 安装 UE 编译依赖
          apt-get install -y build-essential clang cmake python3 libicu-dev libssl-dev zlib1g-dev
          # 安装 .NET 6.0
          wget https://dot.net/v1/dotnet-install.sh -O dotnet-install.sh
          chmod +x dotnet-install.sh
          ./dotnet-install.sh --channel 6.0 --install-dir /usr/share/dotnet
          ln -s /usr/share/dotnet/dotnet /usr/bin/dotnet
          git lfs install

      - name: System Diagnostics (Network & I/O)
        run: |
          echo "📊 --- Disk Write Speed (DD) ---"
          # 写入 512MB 数据测试磁盘写入速度 (oflag=direct 绕过缓存)
          dd if=/dev/zero of=./speedtest.tmp bs=1M count=512 oflag=direct
          rm ./speedtest.tmp

          echo "🌐 --- Network Check to Gitea Host ---"
          TARGET_IP="172.18.0.1"
          ping -c 4 $TARGET_IP || echo "Ping failed"
          
          echo "⬇️ --- HTTP Connection Speed ---"
          # 测试到 Gitea 端口的连接速度
          curl -s -w "Connect: %{time_connect}s\nStartTransfer: %{time_starttransfer}s\nTotal: %{time_total}s\nSpeed: %{speed_download} bps\n" -o /dev/null http://$TARGET_IP:3000/ || echo "Curl failed"

      - name: Manual Checkout Code
        # 使用环境变量直接拉取本地 Gitea 的代码，不走 GitHub
        run: |
          # --- 准备工作 ---
          git init
          TARGET_IP="172.18.0.1"
          # 构造带 Token 的 URL
          AUTH_URL="http://${{ gitea.actor }}:${{ secrets.GITHUB_TOKEN }}@${TARGET_IP}:3000/${{ gitea.repository }}.git"

          # --- 关键优化 1: 禁用 Checkout 时的 LFS 自动下载 ---
          # 这行命令能把 3 小时的等待变成 3 秒！我们稍后手动拉取资源。
          export GIT_LFS_SKIP_SMUDGE=1

          # --- 关键修正 2: 添加 origin ---
          # 先添加远程仓库，这样 git lfs pull origin 就不报错了
          git remote add origin "$AUTH_URL"

          # --- 拉取代码 ---
          echo "🚀 1. 极速拉取代码 (不含大文件)..."
          git fetch --depth 1 origin ${{ gitea.sha }}
          git checkout FETCH_HEAD

          # --- 手动拉取 LFS ---
          echo "🔧 2. 配置 LFS 加速..."
          git lfs install
          # 显式指定 LFS 接口地址
          git config lfs.url "$AUTH_URL/info/lfs"
          # 优化 1: 降低并发，避免服务端过载 (从 8 降为 4)
          git config lfs.concurrenttransfers 4
          # 优化 2: 增大 HTTP 缓冲区，减少分包
          git config http.postBuffer 524288000
          # 优化 3: 禁用 LFS 锁校验，减少 API 请求
          git config lfs.locksverify false
          # 增加超时容忍
          git config lfs.activitytimeout 120

          echo "📦 3. 开始高速下载 LFS 资源 (带进度条)..."
          # 这次下载会有详细的进度显示
          git lfs pull origin
      - name: Verify LFS Assets
        run: |
          echo "检查核心资产大小..."
          # 找一个项目里已知的大资产文件（比如某张贴图或模型）
          # 如果只有几百字节，说明 LFS 还是没拉下来
          find . -name "*.uasset" | head -n 20 | xargs ls -lh
      - name: Check Files
        run: |
          echo "当前工作目录内容："
          ls -R | head -n 20 || true
          find . -name "*.uproject"
          
      - name: Fix Engine Permissions
        run: |
          chmod +x /opt/UE/Engine/Build/BatchFiles/RunUAT.sh
          chmod +x /opt/UE/Engine/Binaries/DotNET/UnrealBuildTool/UnrealBuildTool
          chmod -R +x /opt/UE/Engine/Binaries/Linux/

      - name: Compile and Build
        run: |
          PROJECT_PATH=$(find ${{ github.workspace }} -name "*.uproject" -print -quit)
          
          /opt/UE/Engine/Build/BatchFiles/RunUAT.sh \
          BuildCookRun \
          -project="$PROJECT_PATH" \
          -targetplatform=Linux \
          -clientconfig=Development \
          -build -cook -stage -archive \
          -archivedirectory="${{ github.workspace }}/Saved/Builds" \
          -noP4 -utf8output -nocompileeditor \
          -unattended -stdlog  # <--- 增加这两项来抓取详细 Cook 报错